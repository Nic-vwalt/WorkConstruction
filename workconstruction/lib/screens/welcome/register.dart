import 'package:flutter/material.dart';

class Register extends StatelessWidget {
  final String phoneNumber;
  const Register(this.phoneNumber);

  @override
  Widget build(BuildContext context) {}
}
